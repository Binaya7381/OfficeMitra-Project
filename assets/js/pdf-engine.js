/* ==========================================
   OfficeMitra PDF Engine v1.0
   Powered by pdf-lib
========================================== */

class PDFEngine {

    /* Load PDF */

    static async load(file) {}

    /* Get Page Count */

    static async getPageCount(file) {}

    /* Merge PDFs */

    static async merge(files) {}

    /* Split PDF */

    static async split(file, pages) {}

    /* Rotate Pages */

    static async rotate(file, pages, angle) {}

    /* Delete Pages */

    static async deletePages(file, pages) {}

    /* Extract Pages */

    static async extractPages(file, pages) {}

}

window.PDFEngine = PDFEngine;